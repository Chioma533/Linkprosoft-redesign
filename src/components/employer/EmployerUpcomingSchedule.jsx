import React from "react";
import { FiBriefcase } from "react-icons/fi";

const filters = ["Today", "This week", "This month"];
const EmployerUpcomingSchedule = ({ jobs, filter, setFilter, onViewProject }) => <><div className="hidden md:block bg-white rounded-3xl border border-gray-100 p-6 lg:p-8"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between mb-6"><h3 className="text-base font-bold text-gray-900">Upcoming Schedule</h3><div className="flex items-center gap-1 bg-gray-50 border border-gray-100 rounded-full p-1">{filters.map((item) => <button key={item} onClick={() => setFilter(item)} className={`px-3 py-1 rounded-full text-xs font-semibold ${filter === item ? "bg-white text-gray-900 shadow-sm" : "text-gray-400"}`}>{item}</button>)}</div></div>{jobs.length ? <div className="overflow-x-auto"><table className="w-full min-w-[600px] text-left text-xs"><thead><tr className="border-b border-gray-50 text-gray-400"><th className="pb-3 pr-4">Order ID</th><th className="pb-3 pr-4">Job title</th><th className="pb-3 pr-4">Time</th><th className="pb-3 pr-4">Professional</th><th className="pb-3">Actions</th></tr></thead><tbody className="divide-y divide-gray-50">{jobs.map((job) => <tr key={job.id}><td className="py-4 pr-4 font-semibold text-gray-500">{job.id}</td><td className="py-4 pr-4 font-bold text-gray-800">{job.title}</td><td className="py-4 pr-4 text-gray-500">{job.scheduledTime || "—"}</td><td className="py-4 pr-4 font-semibold text-gray-800">{job.professional}</td><td className="py-4"><button onClick={() => onViewProject(job.id)} className="px-3 py-1.5 rounded-full text-xs font-bold bg-sky-50 text-[#016EA6]">View Details</button></td></tr>)}</tbody></table></div> : <div className="py-10 text-center"><FiBriefcase className="w-7 h-7 text-gray-300 mx-auto mb-2" /><p className="text-xs font-bold text-gray-700">No upcoming schedule</p><p className="text-[11px] text-gray-400 mt-0.5">Jobs in progress will appear here.</p></div>}</div><div className="md:hidden bg-white border border-gray-100 rounded-3xl p-5 space-y-4"><div className="flex items-center justify-between"><h3 className="text-base font-bold text-gray-900">Upcoming Schedule</h3><div className="flex gap-1 bg-gray-50 border border-gray-100 rounded-full p-0.5">{filters.map((item) => <button key={item} onClick={() => setFilter(item)} className={`px-2 py-1 rounded-full text-[10px] font-semibold ${filter === item ? "bg-white text-gray-900 shadow-sm" : "text-gray-400"}`}>{item}</button>)}</div></div>{jobs.length ? <div className="space-y-3">{jobs.map((job) => <div key={job.id} className="flex items-center justify-between p-3 bg-gray-50 rounded-2xl"><div className="min-w-0"><p className="text-xs font-bold text-gray-800 truncate">{job.title}</p><p className="text-[10px] text-gray-400">{job.id}</p></div><button onClick={() => onViewProject(job.id)} className="ml-3 shrink-0 px-3 py-1.5 rounded-full text-[10px] font-bold bg-sky-50 text-[#016EA6]">View Details</button></div>)}</div> : <p className="text-xs text-gray-400 py-4 text-center">No jobs in progress.</p>}</div></>;

export default EmployerUpcomingSchedule;