const verificationService = {
  // Add API methods here when the backend endpoints are confirmed.
  //
  // Example:
  //
  // getVerificationStatus: async () => {
  //   return api.get("/verification/status");
  // },
  //
  // submitIdentity: async (data) => {
  //   return api.post("/verification/identity", data);
  // },
  //
  // submitFaceVerification: async (data) => {
  //   return api.post("/verification/face", data);
  // },
  //
  // submitPaymentVerification: async (data) => {
  //   return api.post("/verification/payment", data);
  // },
};

export default verificationService;
